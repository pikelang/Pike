#!/usr/local/bin/pike

inherit "lib.pike";

int main(int argc, array(string) argv)
{
  argv[0]=find_next_in_path(argv[0],"wc");
  if(file_stat(argv[-1]+".exe"))
    argv[-1]+=".exe";
  exece(argv[0],argv[1..]);
  exit(69);
}
