class GTK2.WindowGroup;
inherit G.Object;

//! Limit the effect of grabs.

void create();
//! Creates a new GTK2.WindowGroup object.  Grabs added with GTK2.grab_add() only
//! affect windows with the same GTk.WindowGroup.

void add_window(GTK2.Window window);
//! Add a window.

void remove_window(GTK2.Window window);
//! Remove a window.

