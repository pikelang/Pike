struct file_ret *aap_find_file( char *s, int len, 
                                char *ho, int hlen, 
                                struct filesystem *f );

void set_filesystem( struct args *a, struct pstring loc )
{
}
